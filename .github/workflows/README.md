# CI/CD Workflow

Этот workflow автоматически проверяет код при каждом push и pull request.

## 🔧 Настройка

### Необходимые секреты в GitHub

Для работы тестов необходимо добавить следующие секреты в настройках репозитория:

1. Перейдите в `Settings` → `Secrets and variables` → `Actions`
2. Добавьте следующие секреты:

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (для тестирования команд)
- `TEST_USER_ID` - ID пользователя Telegram для тестирования (можно получить у @userinfobot)

## 📋 Что проверяется

### 1. Линты (`lint`)
- Проверка кода через ESLint
- Проверка типов TypeScript

### 2. Сборка (`build`)
- Компиляция TypeScript в JavaScript
- Проверка наличия артефактов сборки

### 3. Тестирование команд (`test-commands`)
- Проверка работы всех команд бота:
  - `/start`
  - `/help`
  - `/premium`
  - `/settings`
  - `/stats`
  - `/memory`
  - `/info`

### 4. Проверка работоспособности (`health-check`)
- Проверка подключения к Telegram API
- Проверка наличия необходимых файлов
- Проверка переменных окружения
- Проверка работы команд

### 5. Стресс-тест (`stress-test`)
- Запускается только при push в `main` или `beta-2.0.0`
- Тестирует бота под нагрузкой (5 итераций, 3 параллельных запроса на команду)
- Проверяет производительность и стабильность

## 🚀 Локальный запуск тестов

```bash
# Проверка линтов
npm run lint

# Проверка типов
npm run typecheck

# Сборка
npm run build

# Тестирование команд (требует TELEGRAM_BOT_TOKEN и TEST_USER_ID)
npm run test:commands

# Проверка работоспособности
npm run test:health

# Стресс-тест
npm run test:stress
```

## 📊 Критерии успеха

- ✅ Все линты проходят без ошибок
- ✅ Проект успешно собирается
- ✅ Все команды бота работают корректно
- ✅ Проверка работоспособности проходит успешно
- ✅ Стресс-тест показывает успешность > 90% и среднее время ответа < 5 секунд

