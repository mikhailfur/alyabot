name: Telegram Bot CI

on:
  push:
    branches:
      - beta-2.0.0
      - main
      - master
  pull_request:
    branches:
      - beta-2.0.0
      - main
      - master
  workflow_dispatch:

jobs:
  lint-and-typecheck:
    name: Линтеры и проверка типов
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type check
        run: npm run typecheck

      - name: ESLint check
        run: npm run lint -- --max-warnings 100

      - name: Build project
        run: npm run build

  stress-test:
    name: Стресс-тест
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: lint-and-typecheck

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Create .env file for tests
        run: |
          echo "TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}" > .env
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
          echo "GEMINI_API_KEYS=${{ secrets.GEMINI_API_KEY }}" >> .env

      - name: Run stress test
        run: npm run test:stress
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

  bot-health-check:
    name: Проверка работоспособности бота
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: lint-and-typecheck

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Create .env file for tests
        run: |
          echo "TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}" > .env
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
          echo "GEMINI_API_KEYS=${{ secrets.GEMINI_API_KEY }}" >> .env

      - name: Health check
        run: npm run test:health
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

  test-summary:
    name: Итоговый отчёт
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, stress-test, bot-health-check]
    if: always()

    steps:
      - name: Check results
        run: |
          echo "## Результаты тестирования 🤖" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Тест | Статус |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Линтеры и типы | ${{ needs.lint-and-typecheck.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Стресс-тест | ${{ needs.stress-test.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Проверка работоспособности | ${{ needs.bot-health-check.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.lint-and-typecheck.result }}" != "success" ] || \
             [ "${{ needs.stress-test.result }}" != "success" ] || \
             [ "${{ needs.bot-health-check.result }}" != "success" ]; then
            echo "❌ Некоторые тесты не прошли" >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "✅ Все тесты прошли успешно!" >> $GITHUB_STEP_SUMMARY
          fi
